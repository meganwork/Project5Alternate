#include "Closet_Monster.h"
#include "Under_Bed_Monster.h"
#include <ctime>
#include <iostream>
#include <memory>
#include <vector>
using namespace std;

int main() {
    /*
     * TODO: Create a vector of Darkness Monster unique pointers and push a variety of ten Closet Monsters and Under Bed Monsters to it.
     * TODO: Tell the user that there are monsters everywhere!
     * TODO: Loop through the vector and call the scare method on each. Print the return values.
     *     - This is polymorphism. Why? Answer in your README file.
     * TODO: Repeatedly give the user options to poke the eyes or run away.
     *     - If they poke the eyes, loop through the vector and call the poke_eye method on each.
     *         - This is NOT polymorphism. Why? Answer in your README file.
     *         - After poking all the eyes, loop through the vector again and output the scare methods again.
     *     - If they choose to run away, the program ends.
     */
    return 0;
}

